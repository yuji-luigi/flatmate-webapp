import { Card } from '@mantine/core';

type UserLoginProps = {};

export const UserLogin: React.FC<UserLoginProps> = (props: UserLoginProps) => {
  return (
    <>
      <Card>hey</Card>
    </>
  );
};
